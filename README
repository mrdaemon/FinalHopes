Simple RTS in console

TODO:
	Window focus.
	danse like a chickens.
	finish stats window.
	make Splash screen.
	make banner.
	Start chat window.
	start choosing channels window.
	start browse game window.
	start help window.
	start option window.
	make chat server (on DB).
	make base stats server (on DB).
	make base history server (on DB).
	make chat.
	make stats.
	make history.
	//for a real long time
	make base game server (on DB).
	make game.
	Enjoy code.
	Optimize.
	Grap a smoke.
	Smoke.
	Feel good.
	Trollface.
